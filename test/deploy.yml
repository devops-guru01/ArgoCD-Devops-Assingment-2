apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: "2024-05-01T12:11:18Z"
  labels:
    app: test
  name: test
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: test
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: test
    spec:
      containers:
      - image: test:devops
        imagePullPolicy: IfNotPresent
        name: test
        ports:
        - containerPort: 5000
          protocol: TCP
